package edu.pdx.cs.joy.mob2;

public class BoardClass {
    BoardClass(char[][] inputBoard) {
        this.inputBoard = inputBoard;
    }

    char[][] resultBoard;
    char[][] inputBoard;

    char[][] incrementBoard() {
        for (int row = 0; i < inputBoard.length; ++i) {
            for (int column = 0; i < inputBoard[row].length);
        }
    }
}
